---
"@tailwindcss-mangle/config": patch
"@tailwindcss-mangle/core": patch
---

Refresh internal dependency ranges for the published config and core packages, including `fs-extra`, `postcss`, `@vue/compiler-sfc`, and `svelte`, to keep the workspace aligned with the updated toolchain.
